## Server Properties
server:
   port: 8086

spring:
   config:
      import: vault://hospitalmanagementsystem/stage
   datasource:
      username: ${dbusername}
      password: ${dbpassword}
      url: ${dbjdbcurl}

api:
   username: ${api_username}
   password: ${api_password}
   secretKey: ${secret_key}