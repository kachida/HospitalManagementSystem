## Server Properties
server:
   port: 8085

spring:
   config:
      import: vault://secret/hospitalmanagementsystem/stage
   datasource:
      username: ${dbusername}
      password: ${dbpassword}
      url: ${dbjdbcurl}

api:
   username: ${api_username}
   password: ${api_password}
   secretKey: ${secret_key}