## Server Properties
server:
   port: 8081

spring:
   config:
      import: vault://vaultdemo/hospitalmanagementsystem/test
   datasource:
      username: ${dbusername}
      password: ${dbpassword}
      url: ${dbjdbcurl}

api:
   username: ${api_username}
   password: ${api_password}
   secretKey: ${secret_key}